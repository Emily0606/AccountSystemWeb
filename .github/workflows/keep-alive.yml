name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # 每 10 分鐘執行一次

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://accountsystem-070u.onrender.com/v3/api-docs
